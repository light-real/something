**右值引用**
不能修改的值就是右值，右值一般为临时变量。常见的右值有字面常量值，返回右值的表达式。
所谓右值引用就是必须绑定到右值的引用。我们通过&&来获得右值引用。
右值引用有一个重要的性质——只能绑定到一个将要销毁的对象。
因此，我们可以自由地将一个右值引用的资源“移动”到另一个对象中。

**移动构造函数**
与拷贝构造函数不同，移动构造函数不分配任何新的内存
在接管内存后，它将给定对象中的指针都部署为nullptr
这样就完成了从给定对象的移动操作